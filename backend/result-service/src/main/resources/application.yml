server:
  port: 8084

spring:
  application:
    name: result-service
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/ai_dev_quiz}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:change-me}
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
