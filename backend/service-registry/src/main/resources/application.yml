server:
  port: 8761

spring:
  application:
    name: service-registry

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

logging:
  level:
    root: INFO
    com.netflix: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info
